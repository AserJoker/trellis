{
  "name": "trellis",
  "version": "0.0.0",
  "private": true,
  "description": "Model 中心的自举可扩展框架",
  "packageManager": "pnpm@12.5.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
